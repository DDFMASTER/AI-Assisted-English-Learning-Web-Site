# 英语学习网页软件 — 数据库结构说明文档

---

## 一、数据库环境

| 项目        | 说明                                             |
| --------- | ---------------------------------------------- |
| **DBMS**  | MySQL 8.4                                      |
| **存储引擎**  | InnoDB                                         |
| **字符集**   | utf8mb4                                        |
| **排序规则**  | utf8mb4_general_ci                             |
| **数据库名称** | 使用单个数据库，`AAELDB`，Ai-Assisted English Learning。 |

---

## 二、模块与表概览

| 模块   | 表名                        | 说明               |
| ---- | ------------------------- | ---------------- |
| 用户   | `user`                    | 用户账户与学习信息        |
| 词库   | `junior`                  | 初中英语词库           |
|      | `senior`                  | 高中英语词库           |
|      | `cet_4`                   | 四级英语词库           |
|      | `cet_6`                   | 六级英语词库           |
|      | `postgraduate`            | 考研英语词库           |
|      | `toefl`                   | 托福英语词库           |
|      | `vocab_test`              | 词汇量测试静态表         |
|      | `ai_word_dic`             | AI 动态生成的词典（精讲缓存） |
| 文章   | `article`                 | 英语范文库            |
|      | `user_learned_article`    | 用户学习文章记录         |
| 题库   | `quiz_vocabulary`         | 词汇题库             |
|      | `quiz_comprehension`      | 阅读理解题库           |
| 行为管理 | `ai_word_vote`            | 用户对 AI 词语解释投票    |
|      | `ai_article_explain_vote` | 用户对文章讲解投票        |
|      | `ai_vocquiz_vote`         | 用户对词汇题投票         |
|      | `ai_comquiz_vote`         | 用户对阅读理解题投票       |
|      | `admin_action_log`        | 管理员操作日志          |

---

## 三、表结构详细说明

### 3.1 用户模块

#### `user` 用户信息表

| 字段              | 类型                                  | 约束                                  | 说明                |
| --------------- | ----------------------------------- | ----------------------------------- | ----------------- |
| `user_id`       | BIGINT                              | PK, AUTO_INCREMENT                  | 用户唯一ID            |
| `username`      | VARCHAR(16)                         | NOT NULL, UNIQUE KEY `uk_username`  | 登录用户名，≤16字符       |
| `password`      | CHAR(32)                            | NOT NULL                            | 加盐MD5加密密文         |
| `salt`          | CHAR(10)                            | NOT NULL                            | 密码盐值（10位随机英文字符串）  |
| `profile`       | VARCHAR(127)                        | DEFAULT NULL                        | 个人简介              |
| `role`          | ENUM('normal','vip','admin')        | NOT NULL, DEFAULT 'normal'          | 用户角色              |
| `study_purpose` | ENUM('初中','高中','四级','六级','考研','托福') | NOT NULL                            | 当前学习阶段            |
| `literacy`      | INT                                 | NOT NULL, DEFAULT 0                 | 词汇量测试得分 (0-20000) |
| `last_literacy` | DATETIME                            | DEFAULT NULL                        | 最近一次词汇量测试时间       |
| `experience`    | INT                                 | NOT NULL, DEFAULT 0                 | 经验值               |
| `last_checkin`  | DATETIME                            | DEFAULT NULL                        | 最近一次签到时间          |
| `created_at`    | DATETIME                            | NOT NULL, DEFAULT CURRENT_TIMESTAMP | 注册时间              |
| `last_login`    | DATETIME                            | DEFAULT NULL                        | 最近登录时间            |

**索引与约束**：

- PRIMARY KEY (`user_id`)
- UNIQUE KEY `uk_username` (`username`)

---

### 3.2 词库模块

#### 六张学习阶段词库表（`junior`, `senior`, `cet_4`, `cet_6`, `postgraduate`, `toefl`）

六张表结构完全一致，以下以 `junior` 为例：

| 字段            | 类型           | 约束                         | 说明      |
| ------------- | ------------ | -------------------------- | ------- |
| `id`          | BIGINT       | PK, AUTO_INCREMENT         | 流水主键    |
| `word`        | VARCHAR(64)  | NOT NULL, INDEX `idx_word` | 单词      |
| `phonetic`    | VARCHAR(64)  | DEFAULT NULL               | 英式音标    |
| `translation` | VARCHAR(255) | DEFAULT NULL               | 该阶段单词释义 |

**索引与约束**：

- PRIMARY KEY (`id`)
- INDEX `idx_word` (`word`)

> **设计要点**：  
> 同一单词在同一阶段内可能存在多条记录（不同释义），因此不再将 `word` 设为主键或唯一键，而是使用自增 `id` 作为主键，并对 `word` 建立普通索引以加速查询。

#### `vocab_test` 词汇量测试表（静态数据）

| 字段          | 类型          | 约束                             | 说明                                                |
| ----------- | ----------- | ------------------------------ | ------------------------------------------------- |
| `vocab_id`  | INT         | PK                             | 词汇频率次序 ID（>20000 为伪词） |
| `word`      | VARCHAR(64) | NOT NULL, UNIQUE KEY `uk_word` | 测试单词                                              |
| `is_pseudo` | TINYINT(1)  | NOT NULL, DEFAULT 0            | 是否伪词（0=真词，1=伪词）                                   |

**索引与约束**：

- PRIMARY KEY (`vocab_id`)
  
  > **设计要点**：  
  > 词汇量测试表中同一单词可能出现多次，因此不需要word键唯一。

#### `ai_word_dic` AI 动态词典（词语精讲缓存）

| 字段              | 类型           | 约束                                  | 说明                     |
| --------------- | ------------ | ----------------------------------- | ---------------------- |
| `aidic_id`      | BIGINT       | PK, AUTO_INCREMENT                  | 词典条目 ID                |
| `word`          | VARCHAR(64)  | NOT NULL                            | 单词拼写                   |
| `phonetic`      | VARCHAR(64)  | DEFAULT NULL                        | AI 生成的音标               |
| `translation`   | VARCHAR(255) | DEFAULT NULL                        | AI 生成的简短释义             |
| `explanation`   | TEXT         | DEFAULT NULL                        | AI 词语精讲内容（词义、例句、文化含义等） |
| `like_count`    | INT          | NOT NULL, DEFAULT 0                 | 点赞数                    |
| `dislike_count` | INT          | NOT NULL, DEFAULT 0                 | 不喜欢数                   |
| `is_removed`    | TINYINT(1)   | NOT NULL, DEFAULT 0                 | 软删除标记（1=已删除）           |
| `created_at`    | DATETIME     | NOT NULL, DEFAULT CURRENT_TIMESTAMP | 创建时间                   |

**索引与约束**：

- PRIMARY KEY (`aidic_id`)
- INDEX `idx_word_active` (`word`, `is_removed`, `created_at` DESC)  
  *用于快速获取某单词最新的有效解释记录*

---

### 3.3 文章模块

#### `article` 英语范文库

| 字段                          | 类型                                  | 约束                  | 说明                    |
| --------------------------- | ----------------------------------- | ------------------- | --------------------- |
| `article_id`                | BIGINT                              | PK, AUTO_INCREMENT  | 文章唯一 ID               |
| `title`                     | VARCHAR(127)                        | NOT NULL            | 文章标题                  |
| `content`                   | MEDIUMTEXT                          | NOT NULL            | 文章正文                  |
| `source`                    | VARCHAR(127)                        | DEFAULT NULL        | 文章来源                  |
| `difficulty`                | ENUM('初中','高中','四级','六级','考研','托福') | NOT NULL            | 难度等级（与用户学习阶段对应）       |
| `article_like_count`        | INT                                 | NOT NULL, DEFAULT 0 | 文章内容点赞数               |
| `explanation`               | TEXT                                | DEFAULT NULL        | AI 生成的文章整体解读（首次请求时填充） |
| `explanation_like_count`    | INT                                 | NOT NULL, DEFAULT 0 | 文章讲解点赞数               |
| `explanation_dislike_count` | INT                                 | NOT NULL, DEFAULT 0 | 文章讲解点踩数               |
| `vocquiz_num`               | INT                                 | NOT NULL, DEFAULT 0 | 已关联的词汇题数量             |
| `comquiz_num`               | INT                                 | NOT NULL, DEFAULT 0 | 已关联的阅读理解题数量           |

**索引与约束**：

- PRIMARY KEY (`article_id`)
- INDEX `idx_difficulty` (`difficulty`)

#### `user_learned_article` 用户学习文章记录

| 字段           | 类型       | 约束                                  | 说明     |
| ------------ | -------- | ----------------------------------- | ------ |
| `user_id`    | BIGINT   | PK (联合主键)                           | 用户 ID  |
| `article_id` | BIGINT   | PK (联合主键)                           | 文章 ID  |
| `learned_at` | DATETIME | NOT NULL, DEFAULT CURRENT_TIMESTAMP | 完成学习时间 |

**索引与约束**：

- PRIMARY KEY (`user_id`, `article_id`)
- INDEX `idx_article` (`article_id`)

---

### 3.4 题库模块

#### `quiz_vocabulary` 词汇题库

| 字段              | 类型          | 约束                                  | 说明                |
| --------------- | ----------- | ----------------------------------- | ----------------- |
| `vocquiz_id`    | BIGINT      | PK, AUTO_INCREMENT                  | 词汇题 ID            |
| `article_id`    | BIGINT      | NOT NULL                            | 所属文章 ID           |
| `word`          | VARCHAR(64) | NOT NULL                            | 考察的单词             |
| `content`       | JSON        | NOT NULL                            | AI 生成的题干、选项、答案、解析 |
| `like_count`    | INT         | NOT NULL, DEFAULT 0                 | 点赞数               |
| `dislike_count` | INT         | NOT NULL, DEFAULT 0                 | 不喜欢数              |
| `is_removed`    | TINYINT(1)  | NOT NULL, DEFAULT 0                 | 软删除标记             |
| `created_at`    | DATETIME    | NOT NULL, DEFAULT CURRENT_TIMESTAMP | 生成时间              |

**索引与约束**：

- PRIMARY KEY (`vocquiz_id`)
- INDEX `idx_article_removed` (`article_id`, `is_removed`)
- INDEX `idx_word` (`word`)

#### `quiz_comprehension` 阅读理解题库

| 字段              | 类型         | 约束                                  | 说明                |
| --------------- | ---------- | ----------------------------------- | ----------------- |
| `comquiz_id`    | BIGINT     | PK, AUTO_INCREMENT                  | 阅读理解题 ID          |
| `article_id`    | BIGINT     | NOT NULL                            | 所属文章 ID           |
| `content`       | JSON       | NOT NULL                            | AI 生成的题干、选项、答案、解析 |
| `like_count`    | INT        | NOT NULL, DEFAULT 0                 | 点赞数               |
| `dislike_count` | INT        | NOT NULL, DEFAULT 0                 | 不喜欢数              |
| `is_removed`    | TINYINT(1) | NOT NULL, DEFAULT 0                 | 软删除标记             |
| `created_at`    | DATETIME   | NOT NULL, DEFAULT CURRENT_TIMESTAMP | 生成时间              |

**索引与约束**：

- PRIMARY KEY (`comquiz_id`)
- INDEX `idx_article_removed` (`article_id`, `is_removed`)

---

### 3.5 用户行为管理模块

#### `ai_word_vote` 用户对 AI 词语解释投票

| 字段          | 类型       | 约束                                  | 说明                |
| ----------- | -------- | ----------------------------------- | ----------------- |
| `user_id`   | BIGINT   | PK (联合主键)                           | 投票用户 ID           |
| `aidic_id`  | BIGINT   | PK (联合主键)                           | AI 词典条目 ID        |
| `action`    | TINYINT  | NOT NULL                            | 投票动作：0=赞，1=踩，2=取消 |
| `timestamp` | DATETIME | NOT NULL, DEFAULT CURRENT_TIMESTAMP | 投票时间              |

**索引与约束**：

- PRIMARY KEY (`user_id`, `aidic_id`)
- INDEX `idx_aidic_id` (`aidic_id`)

#### `ai_article_explain_vote` 用户对文章讲解投票

| 字段           | 类型       | 约束                                  | 说明           |
| ------------ | -------- | ----------------------------------- | ------------ |
| `user_id`    | BIGINT   | PK (联合主键)                           | 用户 ID        |
| `article_id` | BIGINT   | PK (联合主键)                           | 文章 ID        |
| `action`     | TINYINT  | NOT NULL                            | 0=赞，1=踩，2=取消 |
| `timestamp`  | DATETIME | NOT NULL, DEFAULT CURRENT_TIMESTAMP | 投票时间         |

**索引与约束**：

- PRIMARY KEY (`user_id`, `article_id`)
- INDEX `idx_article_id` (`article_id`)

#### `ai_vocquiz_vote` 用户对词汇题投票

| 字段           | 类型       | 约束                                  | 说明           |
| ------------ | -------- | ----------------------------------- | ------------ |
| `user_id`    | BIGINT   | PK (联合主键)                           | 用户 ID        |
| `vocquiz_id` | BIGINT   | PK (联合主键)                           | 词汇题 ID       |
| `action`     | TINYINT  | NOT NULL                            | 0=赞，1=踩，2=取消 |
| `timestamp`  | DATETIME | NOT NULL, DEFAULT CURRENT_TIMESTAMP | 投票时间         |

**索引与约束**：

- PRIMARY KEY (`user_id`, `vocquiz_id`)
- INDEX `idx_vocquiz_id` (`vocquiz_id`)

#### `ai_comquiz_vote` 用户对阅读理解题投票

| 字段           | 类型       | 约束                                  | 说明           |
| ------------ | -------- | ----------------------------------- | ------------ |
| `user_id`    | BIGINT   | PK (联合主键)                           | 用户 ID        |
| `comquiz_id` | BIGINT   | PK (联合主键)                           | 阅读理解题 ID     |
| `action`     | TINYINT  | NOT NULL                            | 0=赞，1=踩，2=取消 |
| `timestamp`  | DATETIME | NOT NULL, DEFAULT CURRENT_TIMESTAMP | 投票时间         |

**索引与约束**：

- PRIMARY KEY (`user_id`, `comquiz_id`)
- INDEX `idx_comquiz_id` (`comquiz_id`)

#### `admin_action_log` 管理员操作日志

| 字段            | 类型           | 约束                                  | 说明                      |
| ------------- | ------------ | ----------------------------------- | ----------------------- |
| `user_id`     | BIGINT       | NOT NULL                            | 管理员用户 ID                |
| `target_type` | VARCHAR(64)  | NOT NULL                            | 操作对象类型（如 `ai_word_dic`） |
| `target_id`   | BIGINT       | NOT NULL                            | 操作目标 ID                 |
| `action_type` | VARCHAR(64)  | NOT NULL                            | 操作类型（如 `batch_like`）    |
| `parameter`   | VARCHAR(127) | DEFAULT NULL                        | 操作参数                    |
| `timestamp`   | DATETIME     | NOT NULL, DEFAULT CURRENT_TIMESTAMP | 操作时间                    |

**索引与约束**：

- INDEX `idx_admin_time` (`user_id`, `timestamp`)
- INDEX `idx_target` (`target_type`, `target_id`)

---

## 四、核心业务逻辑对应关系

1. **单词查询**  
   
   - 优先查当前学习阶段对应的词库表（如 `junior`），通过 `WHERE word = ?` 返回所有释义（可能多条）。  
   - 若无结果或用户主动要求 AI 详解，则查询 `ai_word_dic`（取 `is_removed=0` 的最新记录）；仍无结果时调用 AI 生成并插入该表。

2. **AI 动态词典维护**  
   
   - `ai_word_dic` 中的每一条解释独立存在，用户可点赞/踩，通过 `ai_word_vote` 记录并更新冗余计数。  
   - 当 `dislike_count` 超过阈值或管理员操作时，设置 `is_removed=1` 进行软删除。

3. **文章学习与讲解**  
   
   - 用户完成阅读后，在 `user_learned_article` 中记录。  
   - 首次请求文章整体讲解时，AI 生成内容并填入 `article.explanation`。  
   - 讲解的点赞/踩通过 `ai_article_explain_vote` 记录，同步更新 `explanation_like_count` 与 `explanation_dislike_count`。

4. **题目动态填充**  
   
   - 每篇文章初始 `vocquiz_num=0`，`comquiz_num=0`。  
   - 用户首次请求题目时，AI 生成 5 道词汇题和 3 道阅读理解题入库，并更新计数器。  
   - 当库存低于一定数量时，动态补充 1 道题后再随机抽取。  
   - 题目点赞/踩及软删除机制与 AI 词典类似。

5. **用户投票机制**  
   
   - 四张投票表的 `action` 字段三态（0/1/2），联合主键保证一人一内容一条记录。  
   - 应用层通过事务同步更新相应内容表的 `like_count` / `dislike_count`。

6. **管理员操作审计**  
   
   - 所有批量操作（如批量点赞、批量删除）均记录至 `admin_action_log`，便于追溯。

---

## 五、注意事项

- 所有与 AI 交互的输入内容均在后端校验，防止提示词注入。
- 投票表等高频写入表建议定期归档或清理无效数据。
- 未使用外键约束，数据一致性由应用层事务保证。
- `vocab_test` 为静态只读表，不应执行更新操作。
- 词库表的 `id` 仅作为物理主键，业务查询仍依赖 `word` 索引。

---

**文档版本**：v1.0  
**最后更新**：2026-06-27
